#
# Stork Packaging
# https://github.com/fizzed/java-stork
#
name: "${artifactId}"
domain: "${groupId}"
display_name: "${artifactId}"
short_description: "${groupId}:${artifactId}"
type: DAEMON
main_class: "fathom.Boot"
platforms: [ WINDOWS, LINUX, MAC_OSX ]
min_java_version: "1.8"
min_java_memory: 1024
symlink_java: true